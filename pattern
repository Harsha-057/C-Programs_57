#include<stdio.h>
int main()
{
	int n,i,s,a;
	scanf("%d",&n);
	for(i=1;i<=n;i++)
	{
		for(s=1;s<n-i;s++)
		{
			printf(" ");
		}
		for(a=1;a<=i;a++)
		{
			printf("%d",a);
		}
		printf("\n");
	}
	return 0;
}